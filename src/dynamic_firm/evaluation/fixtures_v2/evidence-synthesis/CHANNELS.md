# Channel rules

- Supported channels are `direct` and `bulk`; every other value raises `ValueError`.
- A verified, non-urgent `bulk` delivery is `batch`.
- A verified, non-urgent `direct` delivery is `standard`.
