# Private Hermes Agent foundation

Exact source from `https://github.com/NousResearch/hermes-agent` at `89bd0fba903bbfd78b0d99ce6f194863dd01b7e1`.

The `upstream/` tree preserves upstream Python source bytes for diffability. Noruct loads it only inside the first-party isolated foundation worker. No upstream command, product identity, logo, media, dashboard bundle, builtin skill, or optional MCP catalog is distributed by this intake. See `UPSTREAM_MANIFEST.json`, `VENDOR_POLICY.json`, the adjacent `LICENSE`, `THIRD_PARTY_NOTICES.md`, and the canonical source register.
