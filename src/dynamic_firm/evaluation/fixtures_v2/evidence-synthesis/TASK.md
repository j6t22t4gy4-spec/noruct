# Delivery routing

Implement `route_delivery(channel: str, priority: int, verified: bool) -> str` in
`delivery.py`. The channel and priority rules are split across `CHANNELS.md` and
`PRIORITY.md`. Preserve the public API and change only `delivery.py`.
