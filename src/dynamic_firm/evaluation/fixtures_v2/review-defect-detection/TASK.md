# Retry backoff policy

Review `retry_policy.py` against `POLICY.md`, then correct the smallest bounded
implementation defect. Preserve `backoff_delay(attempt, base, cap)` and change
only `retry_policy.py`.
